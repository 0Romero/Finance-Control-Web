import NavBar from "@/components/NavBar";

export default async function FormCategorias() { 

  return ( 
    <main className="flex min-h-screen flex-col items-center">
      <NavBar active="categorias"></NavBar>
    </main>
  )
}